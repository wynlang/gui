// gui — Cross-platform desktop GUI for Wyn
// Wraps SDL2. Requires: brew install sdl2 (macOS) / apt install libsdl2-dev (Linux)
//
// Usage:
//   var win = Gui.window("My App", 800, 600)
//   while Gui.running(win) {
//       Gui.clear(win, 255, 255, 255)
//       Gui.rect(win, 10, 10, 200, 50, 0, 120, 255)
//       Gui.text(win, 20, 20, "Hello GUI!", 0, 0, 0)
//       Gui.present(win)
//   }
//   Gui.close(win)

// TODO: Implement via C bindings to SDL2
// Window management, event loop, drawing primitives, text rendering
// Target: desktop apps on macOS, Linux, Windows

fn main() -> int {
    println("gui package — Cross-platform desktop GUI for Wyn")
    println("Wraps SDL2 for window management, drawing, events")
    println("Platforms: macOS, Linux, Windows")
    println("Install SDL2: brew install sdl2 (macOS) / apt install libsdl2-dev (Linux)")
    return 0
}
